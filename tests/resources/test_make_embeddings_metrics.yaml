---
embeddings:
  metrics:  # these can be any tensorflow.keras.metrics
    - type: tensorflow.keras.metrics.AUC
      parameters:
        name: AUC
    - type: tensorflow.keras.metrics.Recall
      parameters:
        name: Recall
    - type: tensorflow.keras.metrics.Precision
      parameters:
        name: Precision

